<UserControl x:Class="MonitorAppWPF.DbControls.AnimalsDbControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MonitorAppWPF.DbControls"
             mc:Ignorable="d"
             d:DesignHeight="518" d:DesignWidth="857" BorderBrush="Black">
    <Grid x:Name="_grdBigMain" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Background="RosyBrown">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="6*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Button x:Name="button" Content="Customer table" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Top" 
                Margin="10,12,10,10" Width="74" Click="button_Click" />
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid x:Name="_smallGrid" Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition x:Name="_row2" Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <DataGrid x:Name="_tbCustomers" VerticalAlignment="Stretch" Margin="0,5,0,0" SelectionMode="Single" Grid.Row="0" ItemsSource="{Binding}" SelectionChanged="TbCustomers_SelectionChanged"
                          AutoGenerateColumns="false">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Firstname" Binding="{Binding Path=FIRSTNAME}" />
                        <DataGridTextColumn Header="Surname" Binding="{Binding Path=LASTNAME}" />
                        <DataGridTextColumn Header="Age" Binding="{Binding Path=AGE}" />
                    </DataGrid.Columns>
                </DataGrid>
                <DataGrid x:Name="_tbCustomers2" VerticalAlignment="Stretch" Margin="0,10,0,0" SelectionMode="Single" Grid.Row="1" ItemsSource="{Binding}" SelectionChanged="TbCustomers2_SelectionChanged" />
                <DataGrid x:Name="_tbXmlData" VerticalAlignment="Stretch" Margin="0,10,0,0" SelectionMode="Single" Grid.Row="2" ItemsSource="{Binding}" >
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding FIRSTNAME}" />
                    </DataGrid.Columns>
                </DataGrid>
                <Button x:Name="_btnTest" Grid.Row="3" Content="hello" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="0,10,0,0" Grid.RowSpan="2"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
