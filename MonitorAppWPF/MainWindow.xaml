<Window x:Class="MonitorAppWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MonitorAppWPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1050">
    <Grid Margin="0,0,0,75">
        <Button x:Name="_monitorButton" Content="Start" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Margin="10,10,0,0" Click="_monitorButton_Click" />
        <Label x:Name="_cpuUsageLabel" Content="CPU Usage:" HorizontalAlignment="Right" VerticalAlignment="Top" Width="75" Margin="0,10,100,0" />
        <Label x:Name="_currentCpuUsage" Content="{Binding CpuUsage, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Right" VerticalAlignment="Top" Width="75" Margin="0,10,20,0"/>
        <!-- <Button x:Name="_stopButton" Content="Stop" HorizontalAlignment="Right" VerticalAlignment="Bottom" Width="75" Margin="0,0,15,24" Click="_stopButton_Click" /> -->
    </Grid>
</Window>
